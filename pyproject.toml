[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "astralengine"
version = "2025.1003-dev"
description = "Test"

[tool.black]
line-length = 100
